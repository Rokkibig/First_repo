# Legal AI Agent

This repository contains a skeleton implementation of a Legal AI Agent built with FastAPI and LangGraph. It includes placeholder modules for agents, workflows, services, and tools.

## Development

- Install dependencies: `pip install -r requirements.txt`
- Run tests: `pytest`
